import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.jiten.app',
  appName: 'jiten-dictionary',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
